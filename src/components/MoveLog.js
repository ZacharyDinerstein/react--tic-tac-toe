import React from 'react';
import ReactDOM from 'react-dom';

export function MoveLog(props) {
  return (
    <li key={move}>
          <button data-hi="hi" onClick={() => this.jumpTo(move)}>{desc} - {hi}</button>
    </li>
  );
}

        